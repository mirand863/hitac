configfile: "config/config.yml"

workdir: config["workdir"]

include: "rules/download_unite.smk"
include: "rules/extract_unite.smk"

rule all:
    input:
        unite = expand(
            "results/data/unite/{dataset}",
            dataset=config["urls"]["unite"],
        )
